
import 'package:flutter/material.dart';

class BikeContent extends StatelessWidget {
  const BikeContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}